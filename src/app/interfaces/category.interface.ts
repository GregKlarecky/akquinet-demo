import { PostAttribute } from "./post-attribute.interface";

export interface Category {
  "behind the scenes": PostAttribute;
  Community: PostAttribute;
  "Diversity &amp; Inclusion": PostAttribute;
  Social: PostAttribute;
  "WordPress.com": PostAttribute;
}
